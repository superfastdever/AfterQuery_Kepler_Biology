<!-- ===================================================================
     WRITTEN BY THE REPO OWNER, IN THEIR OWN WORDS. NOT BY AI.

     Kepler runs an AI-detection check on this file and rejects flagged
     submissions. See CLAUDE.md, "Authorship". An assistant may tell you
     what this file needs to COVER and review your draft for gaps; it may
     not write the prose.

     Rough or non-native English is fine and is not what the check looks
     for. Over-polished, evenly-cadenced writing is what trips it.

     Cover all of the following, then DELETE this comment block:

       [ ] The goal and the deliverable -- NOT a checklist of steps.
           State what must be true when the work is done and let the agent
           choose the route. A numbered recipe is a common rejection.
       [ ] Every output path, ABSOLUTE and named explicitly
           (e.g. /app/output.json). If the verifier reads it, this file
           must say so. These must match `artifacts` in task.toml exactly.
       [ ] The exact output format: schema, keys, types, units,
           precision, ordering. Ambiguity you resolve in the verifier but
           not here is unfair and fails the run audit.
       [ ] Where the input data lives inside the container, and what it is.
       [ ] Any constraint the verifier enforces -- tolerances, runtime
           limits, forbidden shortcuts. Nothing graded may be a surprise.
       [ ] Context a domain expert would obviously have. Withholding it is
           an artificial handicap, not difficulty.

     Do NOT include: hints specific to your solution, the answer, links to
     it, or any eval-set canary GUID line (see the guidance -- the structure
     check rejects a bundle containing one).
     =================================================================== -->

# TODO: title

TODO: write the brief here.

<!-- The blank line above and the sentence below are mandatory and must be the
     last content in this file. N must equal [agent].timeout_sec from task.toml
     as an integer (9000 by default). Update it if you change that timeout.
     `tools/structure-check.py` verifies this. -->

You have 9000 seconds to complete this task. Do not cheat by using online solutions or hints specific to this task.
